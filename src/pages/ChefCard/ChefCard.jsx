/* eslint-disable no-unused-vars */
import React from 'react';

const ChefCard = (chef) => {
    const {id, chefName} = chef.chef;
    console.log(chefName)
    return (
        <div>
            
        </div>
    );
};

export default ChefCard;